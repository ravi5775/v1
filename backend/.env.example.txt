# A long, random, secret string used for signing JWTs
JWT_SECRET="replace_this_with_a_very_long_and_random_secret_string"

# Port for the backend server to run on
PORT=3001

# Default admin credentials. The backend will create this user on its first run if it doesn't exist.
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=password123

# Optional: MongoDB Atlas Connection String
# If provided, the app will connect to the cloud.
# If left empty, the app will use the local embedded database.
# IMPORTANT: Replace <db_password> with your actual database password in the .env file.
MONGO_URI=mongodb+srv://ravi7pspk:<db_password>@cluster0.7ukmcnv.mongodb.net/?appName=Cluster0
# SECURITY: This secret is used to sign login tokens.
# We have set a default value so the app starts, but you should change this for production.
JWT_SECRET="sri_vinayaka_secure_token_secret_key_998877"

# Port for the backend server
PORT=3001

# Default Admin Credentials
# The system creates this user automatically on first run if it doesn't exist.
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=password123

# -----------------------------
# HYBRID DATABASE CONFIGURATION
# -----------------------------

# 1. ONLINE MODE (Cloud)
# The app attempts to connect here first.
# IMPORTANT: Replace <db_password> with your actual MongoDB Atlas password.
MONGO_URI=mongodb+srv://srivinayakatender_db_user:%40Ravi7pspk@srivinayakatenders.xudvbid.mongodb.net/?appName=srivinayakatenders

# 2. OFFLINE MODE (Local)
# If the internet is down or the above connection fails, 
# the app automatically switches to the embedded local database 
# stored in the 'backend/.mongodb' folder.